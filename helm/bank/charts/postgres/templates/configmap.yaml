apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgres.fullname" . }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
data:
  POSTGRES_DB: {{ .Values.global.postgres.database | quote }}
  POSTGRES_USER: {{ .Values.global.postgres.username | quote }}
