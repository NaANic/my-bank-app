apiVersion: v1
kind: Secret
metadata:
  name: transfer
  labels:
    {{- include "transfer.labels" . | nindent 4 }}
type: Opaque
stringData:
  OAUTH_CLIENT_SECRET: {{ .Values.oauth.clientSecret | quote }}
